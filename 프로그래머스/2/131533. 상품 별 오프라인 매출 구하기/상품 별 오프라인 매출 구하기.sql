-- 코드를 입력하세요
SELECT PRODUCT_CODE, PRICE*SELL AS SALES FROM PRODUCT INNER JOIN (SELECT PRODUCT_ID, SUM(SALES_AMOUNT) AS SELL FROM OFFLINE_SALE GROUP BY PRODUCT_ID) AS S ON PRODUCT.PRODUCT_ID = S.PRODUCT_ID ORDER BY SALES DESC, PRODUCT_CODE